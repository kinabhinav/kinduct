package test;

import java.util.concurrent.TimeUnit;

import org.apache.maven.model.Organization;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.testng.Assert;
import org.testng.annotations.BeforeTest;
import org.testng.annotations.Test;

//import pages.PractitionerLogin;

public class TestLogin { /*
	
	WebDriver driver;
	
	//PractitionerLogin objLogin;
	 
    Organization objorg;
 
     
 
   // @BeforeTest
 
    public void setup(){
 
        driver = new FirefoxDriver();
 
        driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS);
 
        driver.get("https://qa3.kinductdev.com/login");
 
    }
    
   // @Test(priority=0)
    
    public void test_Home_Page_Appear_Correct(){
 
        //Create Login Page object
 
   // objLogin = new PractitionerLogin(driver);
    
   // objLogin = new l
 
    //Verify login page title
 
    objorg = new Organization();
 
    Assert.assertTrue(objorg.getName().equalsIgnoreCase("Kinduct Clinic - Organizations"));
 
  
 
    } */


}
